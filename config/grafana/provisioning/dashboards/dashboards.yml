apiVersion: 1

providers:
  - name: "Bus GPS Dashboards"
    orgId: 1
    folder: "Bus GPS"
    folderUid: "bus-gps"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
